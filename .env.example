# Supabase (client-side — safe to expose the PUBLISHABLE/anon key only)
VITE_SUPABASE_URL=https://pscoehsbcpxnmdgtplon.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_DHC6wqsDBbi8LCCfN7FvhQ_oAdnfamQ

# Used by npm run db:types (server-side tooling only — do NOT prefix with VITE_)
SUPABASE_PROJECT_ID=pscoehsbcpxnmdgtplon

# EIA (U.S. Energy Information Administration) diesel price.
# The key is NOT a client var anymore — the dashboard widget calls the
# /api/diesel Pages Function, which reads a server-side secret so the key
# never reaches the browser. Set it on the Cloudflare Pages project with:
#   wrangler pages secret put EIA_API_KEY --project-name=freightdesk-app
# Get a free key at https://www.eia.gov/opendata/register.php
